---

---
<li class="watch-more-button">
  <div class="frame">
    <a href="/gallery" id="watchmore">
      Watch More
    </a>
  </div>
  <div class="label">
    <p></p>
  </div>
</li>

<style lang="scss">
  .watch-more-button {
    --w: 318px;
    --h: 410px;
  }

  .watch-more-button {
    display: inline-block;
    width: var(--w);
    height: var(--h);
    text-align: center;
    margin: 0 10px;

    .frame {
      display: flex;
      width: 100%;
      height: 80%;
      justify-content: center;
      align-items: center;

      a {
        display: block;

        &#watchmore {
          padding: 32px 24px;
          border: 1px solid var(--white);
          border-radius: 8px;

          &:hover {
              color: var(--white-2);
            border: 1px solid var(--white-2);
            transition: var(--trs-duration);

            &::after {
              color: var(--white-2);
              content: ">>";
            }
          }
        }
      }
    }

    .label {
      height: 20%;
      width: 100%;

      p {
        display: inline-block;
        word-break: break-all;
        overflow-wrap: break-word;
        text-align: center;
        margin: 10px 0;
      }
    }
  }
</style>